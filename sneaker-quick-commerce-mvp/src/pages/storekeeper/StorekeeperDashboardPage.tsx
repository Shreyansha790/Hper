import { mockOrders, mockInventory, mockAnalytics } from '@/lib/mock';
export function StorekeeperDashboardPage() { const pending = mockOrders.filter((o) => o.status === 'placed' || o.status === 'confirmed').slice(0, 5); const lowStock = mockInventory.filter((i) => i.quantity <= i.lowStockThreshold).slice(0, 5);
return <div className="space-y-6 p-6"><h1 className="text-3xl font-bold">Storekeeper Operations</h1><section className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-white p-4 shadow">Incoming Orders: {pending.length}</div><div className="rounded-2xl bg-white p-4 shadow">Low Stock Alerts: {lowStock.length}</div><div className="rounded-2xl bg-white p-4 shadow">Today Delivered: {mockAnalytics.deliveredToday}</div></section><section className="rounded-2xl bg-white p-4 shadow"><h2 className="font-semibold">AI Image Processing Upload</h2><p className="text-sm text-zinc-500">Upload sneaker photo → preview → “Processing image...” → white-background preview.</p><div className="mt-3 grid gap-3 md:grid-cols-3"><div className="rounded-xl border border-dashed p-6 text-sm">1. Upload</div><div className="rounded-xl border border-dashed p-6 text-sm">2. Processing image...</div><div className="rounded-xl border border-dashed p-6 text-sm">3. Processed preview</div></div></section></div>; }
